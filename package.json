{
  "name": "playground-nft-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio playground-nft-subgraph",
    "create-local": "graph create --node http://localhost:8020/ playground-nft-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ playground-nft-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 playground-nft-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}